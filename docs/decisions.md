# Architecture Decisions

> Maintained by Nirvana. Each significant decision logged here by the Writer, then indexed
> into the shared graphify graph. Run /reflect to synthesize from docs/ + graphify.

| Date | Decision | Reason | Agent |
|------|----------|--------|-------|
| 2026-08-17 | Multi-tenant shared schema (`tenant_id` + EF Core global query filter), não schema-per-tenant no MVP | Ponto de partida do doc de arquitetura: barato modelar agora, caro refatorar depois; isolamento lógico basta pro estágio atual | Architect |
| 2026-08-17 | Roadmap quebrado em 9 tasks (docs/tasks/001-009), seguindo as 5 fases do PDF; task 002 (Identity & Access) adiantada antes da 001 (SharedKernel formal) a pedido do usuário | Login precisava de escopo por tenant; versão mínima de `ITenantContext` implementada inline na 002, generalizada depois na 001 | Tech Lead |
| 2026-08-17 | Sem registro público de tenant — seed cria admin + clínica base; admin autenticado cria os demais usuários (dentista/recepção) dentro do próprio tenant | Decisão do usuário: controle centralizado de provisionamento, não é SaaS self-serve | Product Owner |
| 2026-08-17 | Hash de senha: Argon2id (`Konscious.Security.Cryptography`) | Padrão OWASP atual pra dado sensível de saúde/LGPD, resistente a GPU/ASIC cracking; doc citava Argon2/BCrypt como opções | Product Owner |
| 2026-08-17 | JWT de acesso 15min + refresh token 7 dias com rotação, armazenado em tabela própria (`RefreshTokens`) no módulo Identity | Doc pede "JWT de curta duração + refresh token"; rotação evita replay de refresh token roubado | Architect |
| 2026-08-17 | Email de usuário único globalmente na plataforma (não único por tenant) | Login resolve o tenant a partir do próprio email do usuário, sem exigir header/subdomínio na requisição — simplifica o fluxo de autenticação sem abrir mão de isolamento lógico por tenant nos dados | Dev Backend |
| 2026-08-17 | Refresh token usa hash SHA-256 (não Argon2id, diferente da senha) | Refresh token é um segredo de alta entropia gerado pelo servidor, não uma senha escolhida por humano — Argon2id (lento por design, defesa contra brute-force de senha fraca) seria custo de CPU desperdiçado sem ganho de segurança real nesse caso | Dev Backend |
| 2026-08-17 | Prontuário eletrônico é módulo `Records`, restrito a Admin+Dentista (Recepcao excluído) | Dado clínico sensível sob LGPD — princípio de menor privilégio; Recepcao não precisa de acesso a histórico clínico pra operar agenda/cadastro | Product Owner |
| 2026-08-17 | Criptografia de campo (AES-256-GCM) só em `EvolucaoClinica.DescricaoClinica`; odontograma fica em JSONB sem cifra | Descrição clínica é texto livre sensível; odontograma é dado estrutural (mapa dente→status), não LGPD-sensível no mesmo grau — cifrar tudo indiscriminadamente teria custo sem ganho proporcional | Dev Backend |
| 2026-08-17 | Object storage de anexo é `LocalDiskObjectStorageService` (fallback de dev) atrás de `IObjectStorageService` | Sem S3/Azure Blob provisionado nesta fase; ports-and-adapters garante troca de implementação sem tocar Application quando a infra real existir | Dev Backend |
| 2026-08-17 | Trilha de auditoria (`RecordsAuditLog`) grava via `SaveChangesAsync` PRÓPRIO, separado da operação principal — não via Outbox | Outbox resolveria consistência entre módulos/processos (caso do `ConsultaConcluidaEvent`); aqui o requisito é resiliência a falha LOCAL pós-operação, que uma segunda transação implícita já resolve sem a complexidade de um worker externo | Dev Backend |
| 2026-08-17 | Task 005 implementada sem par Dev Backend/QA dedicado — broker implementou e auto-revisou no thread principal | Limite de gasto mensal em subagent atingido durante a sessão; decisão do usuário foi continuar sem QA separado em vez de pausar o roadmap | Broker (decisão do usuário) |
| 2026-08-17 | Tasks 006 (Financeiro particular) e 007 (Convênios) implementadas JUNTAS, no mesmo módulo `Billing`, um único agregado `Fatura` com `TipoFatura` (Particular/Convenio) em vez de dois agregados paralelos | As duas tasks compartilham 100% da estrutura (parcelas, status, comissão); separar em dois agregados duplicaria toda a lógica de parcelamento/pagamento sem ganho real de coesão | Broker |
| 2026-08-17 | `IConvenioAdapter` (ACL) com implementação de referência `ManualConvenioAdapter` — sem integração real com nenhuma operadora | Não há credencial/API de convênio real disponível nesta fase; a porta genérica permite construir e testar todo o resto do fluxo financeiro antes da integração real existir, trocada depois sem tocar Application/Domain | Broker |
| 2026-08-17 | Idempotência de `CreateFaturaFromConsultaConcluidaCommand` garantida por índice único PARCIAL em `AgendamentoId` (`HasFilter WHERE NOT NULL`), não só pelo check `ExistsByAgendamentoIdAsync` na Application | Mesma classe de corrida TOCTOU já documentada (check e commit são operações separadas no tempo); reentrega de mensagem (at-least-once) precisa de garantia no banco, não só na Application | Broker |
| 2026-08-17 | Módulo `Reporting` só com Application+Contracts (sem Domain/Infrastructure) | Módulo puramente de composição de leitura entre outros módulos — não tem dado próprio nem invariante de negócio a proteger; forçar as 4 camadas de todo módulo anterior seria estrutura sem função | Broker |
| 2026-08-17 | Dashboard/relatórios compostos EM TEMPO REAL (3 queries paralelas nos módulos de origem), não via projeção CQRS persistida sincronizada por evento | Sem worker de Outbox/bus de eventos rodando (dívida herdada da task 004), não há mecanismo confiável de sincronização de projeção; composição em tempo real é simples e correta pro volume esperado do MVP | Broker |
| 2026-08-17 | `FaturaConfiguration.Parcelas` remapeada de `HasMany<Parcela>("_parcelas")` (nome do campo) pra `HasMany(f => f.Parcelas)` (propriedade pública) + `UsePropertyAccessMode(Field)` | Bug real: mapear pelo nome do campo cria navegação PARALELA à propriedade pública auto-descoberta pelo EF Core, causando `InvalidOperationException` na construção do Model — invisível em 26 testes que só mockavam o repositório; ver docs/knowledge/errors-aprendidos.md | Broker |
| 2026-08-17 | Hierarquia Tenant→Unidade→Recurso (Fase 5) implementada como extensão ADITIVA do shared-schema existente — sem migrar pra schema-per-tenant | Sem Postgres real disponível pra validar uma migração de isolamento físico nesta sessão; `UnidadeId` opcional entrega o valor de negócio (RBAC por unidade) sem o risco de uma mudança estrutural não testável | Broker (decisão do usuário) |
| 2026-08-17 | Módulo `Tenancy` novo criado (Domain/Application/Infrastructure/Contracts completo) — não só o `ITenantContext` minimalista que já existia desde a task 001 | Doc de arquitetura sempre previu Tenancy como módulo transversal completo ("gestão de clínicas/unidades de uma rede"); a task 001 só cobriu a fundação técnica (filtro de query), não o agregado de negócio `Unidade` | Broker |
| 2026-08-17 | `ProfissionaisController`/`SalasController` criados nesta task, não na 004 | Gap real descoberto: módulo Scheduling nunca teve endpoint de criação pra `Profissional`/`Sala` (só `GetByIdAsync`); sem CRUD, o novo campo `UnidadeId` não teria como ser exercitado fim-a-fim | Broker |
| 2026-08-17 | RBAC de unidade implementado em profundidade só em `Scheduling.ListAgendamentos`, não replicado raso pros demais módulos | Escolha de entregar o padrão completo (forçado pelo controller, testado) num módulo crítico em vez de uma versão superficial em todos; extensão pros demais é direta quando houver demanda real | Broker |
| 2026-08-17 | Scheduling: RowVersion (EF Core `[Timestamp]`) + Redis lock curto (5s) em dois tempos | RowVersion detecta conflito otimista na escrita DB (barato); lock Redis (5s timeout) protege corrida entre requisições concorrentes no slot antes do commit — necessário pra "maior tráfego" | Architect |
| 2026-08-17 | Scheduling: Outbox pattern (tabela + worker) para `ConsultaConcluidaEvent` | Garante entrega eventual: evento fica em fila se RabbitMQ cair; worker processa async (lag de segundos/minutos aceitável); Billing (task 006) consome via MassTransit | Architect |
| 2026-08-17 | Scheduling: Profissional é entidade local, não User do Identity; UserId opcional | Profissional é dado de negócio (agenda), User é identidade/acesso; desacoplamento permite profissional existir sem user ou múltiplos usuários por profissional | Architect |
| 2026-08-17 | Scheduling: Cache de disponibilidade (slots livres, não agendamentos); TTL 1h; chave `scheduling:disponibilidade:{tenantId}:{profissionalId}:{data}` | Menos dados em cache que listar agendamentos; invalidação granular por dia/profissional; miss obriga SELECT + cálculo de gaps (caro, por isso cache) | Architect |
| 2026-08-17 | SharedKernel: `ApplyTenantQueryFilters(ModelBuilder, TContext)` captura o `TContext` (DbContext) no closure do filtro EF Core, não `ITenantContext` (serviço scoped) como a spec original pedia | `IModel` do EF Core é compilado/cacheado uma vez por processo; fechar sobre serviço scoped vazaria o tenant do primeiro request pra todos os seguintes — bug de segurança evitado antes de produção; ver docs/knowledge/errors-aprendidos.md | Dev Backend |
| 2026-08-17 | CPF do paciente único por `(TenantId, Cpf)`, não globalmente — diferente do email do Identity (único global) | Clínicas (tenants) são operações independentes; mesmo CPF pode existir em duas clínicas sem relação entre si, ao contrário de login que precisa resolver tenant só pelo email | Dev Backend |
| 2026-08-17 | `ValidationBehavior<TRequest,TResponse>` extraído de `Identity.Application` para projeto compartilhado novo `src/Shared/Application.Common`, registrado uma única vez em `Program.cs` | Copiar a classe por módulo faria o MediatR registrar o open generic duas vezes, rodando validação 2x por request — bug real e silencioso | Dev Backend |
| 2026-08-17 | Novo projeto `src/Shared/Contracts.Abstractions` com `PagedResult<T>`, `PageRequest`, `IHasPagination`, `IModuleContract` | Formaliza o que a task 002 já usava de forma inline/ad-hoc; qualquer módulo novo consome paginação e contrato de módulo padronizados sem redefinir | Dev Backend |
| 2026-08-17 | Scheduling: `Agendamento` usa `xmin` do Postgres como concurrency token (via convenção Npgsql), não `RowVersion byte[]` que o Architect havia especificado em docs/decisions.md | `xmin` é o padrão nativo recomendado pelo provider Npgsql pra concorrência otimista e mantém o Domain limpo de detalhe de persistência (sem campo `byte[]` artificial na entidade) | Dev Backend |
| 2026-08-17 | Migrations `InitialCreate` geradas pros 7 módulos com DbContext (`dotnet ef migrations add`, design-time, sem instância de Postgres real) — `Microsoft.EntityFrameworkCore.Design` adicionado como PackageReference direto em `OdontoPlatform.Api` (antes só vinha via `PrivateAssets=all` de cada `*.Infrastructure`, insuficiente pro tooling) | O EF Core valida o Model completo (navegações, conversores, índices) no design-time mesmo sem conexão real — reconfirma que o fix de `Fatura.Parcelas` (task 008) e o mapeamento JSONB/cifrado de Records estão corretos; falta só `dotnet ef database update` quando houver Postgres provisionado | Broker |
| 2026-08-18 | Frontend criado (`frontend/`) — React 18 + TypeScript + Vite, Tailwind CSS v4 (`@tailwindcss/vite`, sem PostCSS separado), TanStack Query, Zustand (auth store persistido), React Hook Form + Zod, FullCalendar (`@fullcalendar/react` pinado em `6.1.21` pra bater com `@fullcalendar/core`, `npm install` sozinho puxou `7.x` incompatível) | Doc de arquitetura sempre especificou esse stack; nenhuma tela existia até agora (só backend). Começando pelo módulo Agenda (Calendário) por ser o mais visível/citado | Broker (decisão do usuário) |
| 2026-08-18 | Sprint 6 — Pivot multi-organização: JWT emite SÓ `organization_id` (não `tenant_id` + compatibilidade) | Não há cliente externo; frontend atualizado no mesmo deploy (task 012). Compatibilidade é custo sem benefício | Architect |
| 2026-08-18 | Sprint 6 — Rename (task 010, 011): regerar `InitialCreate` migrations (nunca foram aplicadas contra Postgres real), não migration de `ALTER TABLE RENAME COLUMN` | Migrations de 2026-08-18 são design-time; sem dado em produção; regenerar é mais simples e reduz risco | Architect |
| 2026-08-18 | Sprint 6 — Rename: ordem em 5 etapas (SharedKernel → Identity → Domínios → DTOs/Controllers → Testes/Migrations) pra manter build verde em cada commit | Reduz risco de rebase/merge quebrado; cada etapa é compilável | Architect |
| 2026-08-18 | Sprint 6 — `User` vira entidade GLOBAL (não `IMustHaveOrganization`); `OrganizationMembership` é tenant-scoped (task 013) | Email é único global; afiliação é responsabilidade de Membership, não de User. Simplifica modelo | Architect |
| 2026-08-18 | Sprint 6 — `RefreshToken.OrganizationId` EXISTE (escopado a org eleita no login), mas não duplica `TenantId` — refresh token é do usuário numa org específica | Refresh emitido no login já eleita org; se trocar de org, novo refresh é emitido. Não é portável entre orgs | Architect |
| 2026-08-18 | Sprint 6 — `OrganizationMembership` mora em `Identity.Domain` (junto de User e Organization) | Relação N:N entre agregados do Identity; evita dependência circular cross-module | Architect |
| 2026-08-18 | Sprint 6 — Login com múltiplas memberships: elege a PRIMEIRA CRIADA (mais antiga) como org ativa | Determinístico, sem UI bloqueante. Usuário troca depois via `switch-organization` se quiser. Persistir "última usada" é feature futura | Architect |
| 2026-08-18 | Sprint 6 — Usuário SEM organização recebe token COM `organization_id` claim ausente; policy `RequireActiveOrganization` bloqueia endpoints que exigem org | Fail-closed (sem claim, 403 automático). Endpoints que toleram: `/api/me`, `/api/organizations` (POST), `/api/me/invites`, `POST /api/invites/{token}/accept` | Architect |
| 2026-08-18 | Sprint 6 — `POST /api/organizations` retorna `201 Created` com accessToken + refreshToken já escopados à org criada (uma ida a menos pro cliente) | UX melhorada; padrão consistente com signup | Architect |
| 2026-08-18 | Sprint 6 — Email duplicado em signup retorna `409 Conflict` com mensagem genérica ("Email já em uso"), não `200` silencioso | Enumeração de email já é possível via login; ser inconsistente não protege e piora UX | Architect |
| 2026-08-18 | Sprint 6 — `Invite.TokenHash` usa `Sha256InviteTokenGenerator` (mesmo padrão de RefreshToken) — token armazenado como hash, nunca em claro | Consistência; tokens são segredos | Architect |
| 2026-08-18 | Sprint 6 — `Invite` é tenant-scoped (`IMustHaveOrganization`), mas método `GetPendingByEmailAcrossOrganizationsAsync` ignora filtro EXPLICITAMENTE com documentação inline | Caso legítimo: usuário vê convites de orgs onde NÃO é membro (ainda). Nunca `IgnoreQueryFilters()` solto; método nomeado torna intenção clara | Architect |
| 2026-08-18 | Sprint 6 — Convite duplicado (mesmo email+org, anterior pendente): REVOGAR anterior, criar novo | Workflow simples (Owner redigita sem pensar); revogação é barata (flag) | Architect |
| 2026-08-18 | Sprint 6 — `GET /api/me` embutido `pendingInvites[]` (além de endpoint separado `GET /api/me/invites`) | Uma ida só no boot; frontend recebe tudo (user, orgs, activeOrg, invites) | Architect |
| 2026-08-18 | Sprint 6 — Verificação de email em signup é DÉBITO (dívida técnica pré-produção); signup permite qualquer email sem double opt-in | Aceitável no MVP (sem cobrança); bloqueante antes de qualquer usuário real em produção | Architect |
| 2026-08-18 | Sprint 6 — `IInviteNotifier` com implementação no-op (log token apenas); envio de email real é task futura | Token só em log de servidor; fluxo testável; inutilizável em produção até existir provider | Architect |
| 2026-08-18 | Componentes de UI (`Button`,`Input`,`Select`,`Card`,`Modal`,`StatusBadge`) escritos à mão com Tailwind, não via CLI do shadcn/ui | CLI do shadcn precisa buscar registry externo/setup interativo, impraticável no ambiente headless desta sessão; visual e API seguem a mesma convenção (variant props, `cn()` helper) — trocar por shadcn de verdade depois é direto | Broker |
| 2026-08-18 | CORS liberado em `Program.cs` só pra `http://localhost:5173` (dev server do Vite) | Frontend roda em origem separada do backend (`5262`); produção precisa trocar a origem fixa por config vinda de `appsettings`/env, nunca `AllowAnyOrigin` | Broker |
| 2026-08-18 | `GET /api/profissionais` e `GET /api/salas` (listagem, qualquer autenticado) criados — gap fechado igual ao de `POST` na Fase 5 | Frontend (formulário de novo agendamento) precisa listar profissionais/salas pra popular os selects; só existia `GetByIdAsync` internamente, nenhum endpoint de leitura pública | Broker |
| 2026-08-18 | `zod` instalado na v4 (não v3) — API de erro custom mudou de `z.literal(true, { errorMap })` pra `z.literal(true, { message })` | `npm install zod` sem pin pegou a versão atual (v4); ajustado no schema de consentimento LGPD do formulário de paciente | Broker |
| 2026-08-18 | Validação de CPF (mod-11) duplicada no client (`frontend/src/lib/cpf.ts`) espelhando `Patients.Domain.ValueObjects.Cpf` | Feedback imediato no formulário sem round-trip à API pra erro óbvio; a validação que importa de verdade continua só no backend (client nunca é a fonte da verdade) | Broker |
| 2026-08-18 | `GET /api/convenios` (listagem, Admin+Recepcao) criado — mesmo gap de `Profissionais`/`Salas` na Fase 5 | Frontend precisa listar convênios pro select do formulário de fatura de convênio; só existia `POST` (Admin-only) | Broker |
| 2026-08-18 | Campos numéricos de formulário (RHF+Zod) usam `register(..., { valueAsNumber: true })` em vez de `z.coerce.number()` | `z.coerce` no Zod v4 quebra a inferência de tipo do `zodResolver` (input≠output do schema conflita com o generic do `useForm`) — `valueAsNumber` mantém o tipo do form consistente ponta a ponta | Broker |
| 2026-08-18 | Tela **Estoque** entregue: lista (indicador de estoque baixo), cadastro de item, entrada/saída registradas inline por linha (input de quantidade + 2 botões, sem modal) | Ação mais frequente do dia a dia (movimentar estoque) não justifica o overhead de abrir modal a cada lançamento; cadastro de item (raro) continua em modal | Broker |
| 2026-08-18 | Tela **Relatórios** entregue: date range picker (default = mês corrente) + 3 cards (dashboard geral, faturamento, agenda), consumindo os 3 GETs de `Reporting` já prontos no backend | Fecha o roadmap de 5 fases também no frontend — última tela pendente da Fase 4 (Inteligência) | Broker |
| 2026-08-18 | Badge de status em Estoque não reusa `StatusBadge` (componente existente só tipa `AgendamentoStatus`) — span inline com classes Tailwind direto, mesmo padrão do `FaturaStatusBadge` dedicado | Manter `StatusBadge` genérico exigiria typá-lo por union de todos os domínios ou aceitar `string` solto (perde type-safety); um badge por feature já é o padrão estabelecido na Fase 3 (Financeiro) | Broker |
| 2026-08-18 | Frontend: todas as 5 telas de negócio do roadmap concluídas (Agenda, Pacientes, Financeiro, Estoque, Relatórios) — SPA completa, verificada visualmente via claude-in-chrome com JWT fake injetado no localStorage (sem Postgres real disponível nesta sessão) | Encerra o item pendente "Rede/Fase 5 + Inteligência/Fase 4 no frontend" aberto desde o início da sessão de frontend | Broker |
| 2026-08-18 | Sprint 6 — Execução do rename (010/011) via script `sed` em 3 passes por casing (`TENANT`→`ORGANIZATION`, `Tenant`→`Organization`, `tenant`→`organization`, mesma ordem pra `Unidade`→`Branch`, plural antes de singular), aplicado numa tacada só em `src/`+`tests/`, não editado arquivo-a-arquivo nem em commits por etapa (sem git neste repo) | Volume de 228 arquivos torna edição manual inviável; validação por `dotnet build`/`dotnet test` ao fim de cada fase lógica (SharedKernel+Infra → Identity → módulos → migrations) supriu a falta de boundary de commit por etapa que o Architect havia planejado | Dev Backend |
| 2026-08-18 | Sprint 6 — Falso-cognato `ItemEstoque.UnidadeMedida` protegido via placeholder antes do script de rename `Unidade`→`Branch`, mapeado previamente por grep de todo token contendo a substring-alvo | Rename ingênuo por substring teria transformado `UnidadeMedida` (unidade de medida do item de estoque — kg/un/cx) em `BranchMedida`, trocando um conceito de negócio por outro sem gerar erro de build; ver docs/knowledge/errors-aprendidos.md | Dev Backend |
| 2026-08-18 | Sprint 6 — `switch-organization` NÃO invalida o token (access + refresh) anterior; token antigo permanece válido até expirar sozinho (15min) | Revogar exigiria SQL + cache extra pra um edge case raro (usuário troca de org e insiste em usar o token velho); tokens já são curtos por design, custo de revogar > risco aceito | Architect |
| 2026-08-18 | Sprint 6 — Refresh token só funciona se o token carregar `organization_id`; usuário sem organização não tem rota de refresh — precisa logar de novo ou aceitar um convite primeiro | Evita lógica especial de "refresh mantém null org"; simplifica o contrato (refresh sempre reelege a mesma org do token, nunca decide org do zero) | Architect |
| 2026-08-18 | Sprint 7 — **A1**: `Fatura.CalcularComissaoSobre(decimal valorPago)` mora em Domain (método público), não provider | É cálculo de comissão proporcional — regra de negócio pura (D2 do TL reafirmada). Arredondamento idêntico ao `CalcularValorComissao()` (2 casas, `MidpointRounding.ToEven`) | Architect |
| 2026-08-18 | Sprint 7 — **A2**: Assinaturas dos 3 lookups (Comissao/Profissional/Membership) confirmadas batch-ready, sem N+1; padrão: `Task<IReadOnlyList<Dto>>` | Ver task 022, seções 1-3. Padrão idêntico ao `IFaturamentoSummaryProvider` já existente | Architect |
| 2026-08-18 | Sprint 7 — **A3**: `BranchNome` vem de `Tenancy.Contracts.IBranchLookup.ListarNomesAsync()` novo | Estender IBranchLookup (hoje só tem `ExistsAsync`) com método que retorna dicionário `BranchId → BranchNome`. Implementação em Tenancy.Infrastructure com `AsNoTracking()` + `IgnoreQueryFilters()`. Motivo: desacoplamento — Reporting não depende de Scheduling pra carregar branch | Architect |
| 2026-08-18 | Sprint 7 — **A4**: `UserId → ProfissionalId` resolvido via `IProfissionalLookup.ObterPorUserIdAsync()` novo | Adicionar sobrecarga que retorna `ProfissionalResumoDto?`. Motivo: evita carregar lista inteira de profissionais no controller; lookup único, batch-ready. Dentista sem profissional vinculado retorna `null` (tratado no controller como 200 com totais zerados) | Architect |
| 2026-08-18 | Sprint 7 — **A5**: Dark mode via `.dark` class + `@custom-variant dark` em `index.css` (Tailwind v4 nativo) | ThemeProvider gerencia estado (`light|dark|system`) em Zustand/localStorage. Script inline em `index.html` aplica classe `.dark` ao `<html>` antes de React render (evita FOUC). Fallback: sem localStorage = `@media (prefers-color-scheme: dark)` do SO. Motivo: permite toggle manual + respeita SO; sem `data-theme` extra | Architect |
| 2026-08-18 | Sprint 7 — **A6**: Ícones — adicionar `lucide-react` ao `package.json` **se** task 024 especificar | Nenhuma lib instalada hoje. `lucide-react` é leve, bem mantida, zero deps pesadas, segue design minimalista. Se 024 não pedir, não adicionar | Architect |
| 2026-08-19 | Sprint 7 — **Owner é superset de Admin, não um 5º papel operacional paralelo**: toda checagem de role hoje `Admin`-only pra função ADMINISTRATIVA (cadastro, configuração, relatórios) passa a incluir `Owner`, sem remover nenhum role operacional (`Dentista`,`Recepcao`) já presente | `Owner` é o papel de quem criou a organização (task 015, sprint-6) — é o dono, não um papel à parte e mais restrito. Achado durante a 027 (Dev Frontend: `BranchesController` bloqueava Owner); tratado como hotfix (030) dentro da mesma sprint em vez de esperar sprint nova, por ser bug real de acesso em produção | Product Owner (broker), task docs/tasks/030-owner-role-parity-rbac.md |
| 2026-08-19 | Sprint 7 — Widening de RBAC de um papel (Owner) precisa varrer 4 camadas de gate (backend `[Authorize]`, frontend rota `ProtectedRoute`, frontend menu `AppLayout`, componente inline `role === 'X'`), não só a camada onde o achado original apareceu | A task 030 corrigiu só o backend achando que resolvia por completo; QA achou o mesmo padrão de bug em 4 arquivos do frontend (`App.tsx`, `AppLayout.tsx`, `ReportsPage.tsx`, `ComprovantePage.tsx`) em 2 rodadas de revalidação sucessivas — ver `docs/knowledge/patterns.md` e `docs/knowledge/errors-aprendidos.md` | QA (Jubileu), tasks 027/028/030 |
| 2026-08-19 | Sprint 8 — Redesign da Agenda (task 031) colore evento/legenda por **status**, não por **procedimento**, apesar da imagem de referência do usuário usar procedimento | `Agendamento` (domínio Scheduling) não tem campo de tipo de procedimento — só `status`. Adicionar essa dimensão exigia campo novo + migration, fora da regra de negócio da sprint ("só visual, dado já existe"); confirmado com o usuário em grill-me. Mantido o mapeamento já tokenizado (`statusTone`/`toneClasses`) | Product Owner (decisão do usuário), task docs/tasks/031-agenda-redesign-referencia-visual.md |
| 2026-08-19 | Sprint 8 — `AgendaPage.tsx` ganha 2 lookups client-side extras (`listProfissionais`, `listPatients`) além de `listAgendamentos`, pra resolver nome de paciente/profissional no chip do calendário | Nenhum endpoint novo: as duas queries já existiam (mesma `queryKey` de `NovoAgendamentoModal`), cache do TanStack Query dedupe entre telas — 3 requests na carga da Agenda em vez de 1, aceito como custo baixo (task 031, R9) | Tech Lead |
| 2026-08-19 | Sprint 8 — mapeamento `AgendamentoStatus → tom/label/dot/cor` centralizado em `features/scheduling/status-display.ts`, `StatusBadge.tsx` passou a importar de lá em vez de manter cópia própria | Task 031 precisava do mesmo mapeamento em 4 lugares (badge, chip do calendário, dot da lista de próximos, legenda); duplicar de novo repetiria o risco de drift já registrado no design-system.md §5 pro par `StatusBadge`/`FaturaStatusBadge` | Dev Frontend |
| 2026-08-19 | Sprint 9 — Dark mode reskin: fidelidade de cor às fotos de referência (teal-ciano vívido + gradiente no botão primário + superfícies quase pretas) passa a ter prioridade sobre a regra "não pareça vibe codado" da 024, **só no `.dark`** | Usuário pediu explicitamente 2ª vez "parecido com a foto", desta vez apontando as fotos dark; confirmado em grill-me que isso substitui a preferência antiga da 024 pro modo escuro. Light não foi tocado (usuário não escolheu a foto light nesta rodada) | Product Owner (decisão do usuário), task docs/tasks/032-reskin-dark-mode-fidelidade-fotos.md |
| 2026-08-19 | Sprint 9 — `--color-on-brand` no dark **continua escuro** (`#06170F`), não vira branco como na foto | Branco sobre o novo `--color-brand` (#2DD4BF) dá ~1.9:1 — falha AA. Fidelidade de cor teve prioridade sobre a regra estética da 024, mas não foi pedido abrir mão de contraste AA; mantido escuro (11:1 na ponta teal do gradiente, 5.03:1 na ponta azul) | Architect |
| 2026-08-19 | Sprint 9 — Gradiente do botão primário implementado como par de CSS vars fora do `@theme` (`--grad-brand-from`/`--grad-brand-to` + classe `.bg-brand-gradient`), não como token dentro do `@theme` | Uma var dentro do `@theme` no namespace `--color-*` geraria utilitárias Tailwind (`bg-grad-brand-from` etc.) sem uso — ficaria bloat morto. Fora do `@theme`, é só CSS var comum, referenciada via `var()` numa classe escrita à mão; mesma lógica de "1 var, override só no `.dark`" dos outros tokens, sem gerar utilitária indesejada | Architect |
| 2026-08-19 | Sprint 9 — Item de nav ativo (`AppLayout`) trocou de `bg-brand-subtle text-brand` (pálido) pra `bg-brand text-on-brand` (sólido), nos dois temas | As fotos de referência (dark E a light usada na sprint-8) mostram o item ativo como pill sólido, não pálido — a 024 tinha especificado pálido antes de existir foto de referência pra comparar. Efeito colateral corrigido: badge de contagem de Convites precisou inverter cor (`bg-on-brand text-brand`) quando o próprio item já está sólido, senão ficaria invisível (mesma cor do fundo) | Dev Frontend |
| 2026-08-19 | Repositório subiu pro GitHub (`AndrausP/odontecompany`, público) — senha real do Postgres local (`Andraus66?`, craba em `appsettings.json`) trocada por placeholder `CHANGE-ME`, movida pra `appsettings.Development.json` (novo, gitignored) + `.example` versionado como template | Senha parecia pessoal/reutilizável, não descartável; repo é público, bots varrem GitHub em segundos. Usuário confirmou essa opção em vez de manter privado ou subir como estava | Broker (decisão do usuário) |
| 2026-08-19 | Sprint 10 — Reativação de `OrganizationMembership` (`Reativar(Role role)`) adota o **papel do convite novo**, não preserva o papel antigo da membership desativada | Quem re-convida escolhe o papel explicitamente no convite — preservar o papel antigo (possivelmente obsoleto, ex: a pessoa era Admin e devia voltar só como Recepcao) ignoraria essa escolha explícita do Owner | Architect, task docs/tasks/020-membership-reativar-em-invite.md |
| 2026-08-19 | Sprint 10 — Bootstrap de integração (021): banco de teste **InMemory** (`Microsoft.EntityFrameworkCore.InMemory`), não Postgres efêmero | Objetivo da task é exercitar o pipeline HTTP (auth/policy/rate-limit/routing), não fidelidade de SQL/constraints; Postgres efêmero pediria infraestrutura de CI que o repo ainda não tem, sem ganho pros 5 casos-alvo | Architect, task docs/tasks/021-bootstrap-integration-tests.md |
| 2026-08-19 | Sprint 10 — Bootstrap de integração (021): sem helper de JWT manual — testes usam os 4 endpoints públicos reais (`signup`/`login`/`POST organizations`/`accept invite`) pra obter qualquer token/estado necessário | Mais fiel (testa a emissão real do token, não uma simulação) e evita reimplementar assinatura de JWT no lado do teste; `signup` já devolve exatamente o token "sem organization_id" que o caso 1 precisa | Architect, task docs/tasks/021-bootstrap-integration-tests.md |
| 2026-08-19 | Sprint 10 — `Program.cs` ganhou `public partial class Program {}` no fim do arquivo | `WebApplicationFactory<Program>` (testes de integração) não enxerga a classe `Program` gerada pelos top-level statements de outro assembly sem essa declaração parcial pública — padrão oficial da Microsoft pra esse cenário, zero mudança de comportamento do host | Dev Backend, task docs/tasks/021-bootstrap-integration-tests.md |
| 2026-08-19 | Sprint 11 — Pedido de "polimento" (escopo "tudo") travado com regra de negócio: zero cor hardcoded remanescente = critério de "polido" pra esta rodada; sweep (033) achou **zero** ocorrências — nenhuma implementação foi necessária, task fechou como verificação | `frontend/src` já estava 100% migrado pros tokens desde a 025 (sprint-7); sprints 8/9 (031 Agenda, 032 dark reskin) não reintroduziram cor solta. Achado lateral fora de escopo: bundle principal 792.90 kB sem code-splitting — registrado como possível item de backlog futuro, não corrigido | QA (Jubileu), task docs/tasks/033-sweep-cor-hardcoded-verificacao.md |
| 2026-08-19 | Sprint 11 (034) — Code-splitting via `React.lazy`/`Suspense` nativo do React Router, não via lib de roteamento nova nem split manual por lib pesada dentro de página | 10 páginas de rota em `App.tsx` viraram lazy import; shell (`AppLayout`/`ProtectedRoute`/`RequireOrganization`) continua eager. Bundle principal caiu de 792.90 kB pra 250.79 kB, maior chunk pós-split (`AgendaPage`, FullCalendar) fica em 285.02 kB — ambos abaixo do limite de aviso do Vite (500 kB), sem precisar granularidade maior | Dev Frontend, task docs/tasks/034-code-splitting-rotas.md |
| 2026-08-19 | Sprint 11 (035) — `DateTime` normalizado pra UTC via convenção EF Core (`ConfigureConventions`) nos 7 `DbContext`s, não `SpecifyKind` pontual por handler | Achado validando endpoints a pedido do usuário: `POST /api/patients` 500ava (`Cannot write DateTime with Kind=Unspecified`) porque model binding de JSON nunca marca o `Kind` do `DateTime`; qualquer campo de data de qualquer módulo tinha o mesmo risco latente — corrigido na raiz (convenção), não no sintoma (1 endpoint) — ver docs/knowledge/errors-aprendidos.md e patterns.md | Broker, task docs/tasks/035-fix-datetime-utc-endpoints.md |
| 2026-08-19 | Sprint 11 (035) — `color-scheme` declarado explicitamente (`light`/`.dark{dark}`) em `index.css` | Ícone nativo do date picker (calendário) ficava invisível no tema dark — browser sempre assumia esquema claro pros controles nativos por falta dessa declaração; resolvido sem filter/invert hack em cima do SVG do próprio Chrome | Broker, task docs/tasks/035-fix-datetime-utc-endpoints.md |
| 2026-08-19 | Sprint 11 (036/037) — Onboarding pós-login vira fluxo guiado de 2 passos (Organization → 1ª Branch) com opção de pular ("por enquanto não"), persistida no backend (`User.OnboardingSkipped`, migration nova) — sem criar hierarquia de tenancy nova ("franquia"/"filiado" confirmados pelo usuário como sinônimo informal de Branch, não conceito de domínio) | Grill-me: usuário queria "franquia"/"filiado" mas descreveu exatamente Organization(empresa)/Branch(unidade) já existentes — CreateBranch/ListBranches já prontos no backend (Tenancy, task 030), só faltava o fluxo guiado no frontend + a opção de pular persistida (senão banner de onboarding voltaria a cada novo device/sessão) | Product Owner (decisão do usuário), tasks docs/tasks/036-backend-skip-onboarding.md e 037-onboarding-guiado-org-filial.md |
| 2026-08-19 | Sprint 11 (036) — Sem Branch como gate de navegação: pulando o passo 2 do onboarding (ou organization sem nenhuma branch por qualquer motivo), o app não bloqueia — só a ausência de Organization bloqueia (`AppLayout` mostra estado vazio só quando `organizations.length === 0`) | YAGNI: nenhuma tela hoje quebra com zero branches (mesmo padrão de estado vazio já usado em Pacientes/Agenda com zero registros); adicionar um segundo nível de gate por Branch sem necessidade concreta seria complexidade não pedida | Architect, task docs/tasks/037-onboarding-guiado-org-filial.md |
| 2026-08-19 | Sprint 11 (038) — Landing page pública ("/") com pricing como vitrine estática (3 planos: Starter/Profissional/Rede), sem checkout/gateway de pagamento nessa rodada — todo CTA leva pro `/signup` | Grill-me: escopo da rodada é conversão visitante→signup, não cobrança real; integrar gateway de pagamento é mudança de escopo maior (novo módulo/extensão de Billing), fica pra quando houver decisão de negócio sobre qual gateway usar | Product Owner (decisão do usuário), task docs/tasks/038-landing-page-publica.md |
| 2026-08-19 | Sprint 11 (038) — Rota `/` pública nova: autenticado pula direto pra `/agenda` (`LandingGate`), sem sessão mostra `LandingPage`; catch-all (`path="*"`) trocou de redirect fixo `/agenda` pra `/` | Antes o app não tinha home pública — visitante sem sessão caindo em rota desconhecida batia em `/agenda`→`ProtectedRoute`→`/login` (2 redirects); agora cai direto na landing, sem apresentação nenhuma antes não existia | Dev Frontend, task docs/tasks/038-landing-page-publica.md |
| 2026-08-19 | Sprint 11 (039) — Módulo novo `Subscriptions` (Domain/Application/Infrastructure/Contracts completo, 8º módulo do monólito), separado de `Billing` | `Billing` é faturamento do PACIENTE (paciente paga a clínica); Subscriptions é assinatura da PLATAFORMA (organização paga o OdontoPlatform) — domínios irmãos, mas conceitos de negócio diferentes; misturar no mesmo módulo confundiria as duas coisas num único agregado/DbContext | Architect (grill-me com usuário), task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — 3 planos (`PlanTier` enum + `PlanCatalog` estático), sem tabela/seed de "Plan" no banco | Conteúdo fixo do produto (Starter/Profissional/Rede), não dado administrável — tabela geraria CRUD/seed sem necessidade real nesta fase; trocar preço/limite é mudança de código, aceitável pro estágio atual | Architect, task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — "Bloquear quem não tem plano" implementado como upsert direto (`SelectPlanCommand` cria `Subscription` com `Status=Ativa`, sem cobrança), checkout Stripe real fica em endpoint SEPARADO (`POST /subscriptions/checkout`, esqueleto) | Regra de negócio da rodada é gate de acesso (organização precisa ESCOLHER um plano), não cobrança de verdade — separar os dois endpoints permite o gate funcionar hoje e o checkout virar real depois sem re-desenhar o fluxo de onboarding | Product Owner (decisão do usuário via grill-me), task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — `IPaymentGatewayService`/`StripePaymentGatewayService` é esqueleto que loga e devolve placeholder (nunca `NotImplementedException`), sem `Stripe.net` instalado | Pedido explícito do usuário ("esqueleto, não precisa funcionar ainda") — mesmo padrão já usado em `IConvenioAdapter`/`ManualConvenioAdapter` (Billing); lançar exception quebraria qualquer fluxo que exercite o endpoint antes do Stripe real existir | Dev Backend, task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — Limite de filial por plano verificado no `CreateBranchCommandHandler` (Tenancy.Application), via `ISubscriptionLookup` cross-module (Subscriptions.Contracts) | Mesma camada onde `Branch` já é criada — evita duplicar a checagem no controller (Bootstrap) E no handler; `ISubscriptionLookup` segue a mesma fronteira ACL de `IBranchLookup`/`IPatientLookup`, sem `Tenancy` depender de `Subscriptions.Domain/Infrastructure` | Architect, task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — Onboarding vira 3 passos (Empresa→Plano→Unidade); resume pós-reload trava "tinha organization ao chegar" num `useRef` (não recalculado a cada render) e o guard de atalho do topo só dispara com `step === null` | 2 bugs reais achados testando ao vivo — recalcular o estado "como chegou" a partir do mesmo cache que o passo 1 modifica fazia o fluxo pular o passo de criar unidade; guard sem checar `step` disparava no meio de uma transição deliberada. Ver docs/knowledge/errors-aprendidos.md e patterns.md | Dev Frontend, task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (039) — Organization (Cnpj/Telefone/Endereco) e Branch (Telefone) ganham campos opcionais direto na entidade (migration simples), sem tabela de "perfil estendido" separada | Volume pequeno de campos (3 e 1), sem necessidade de versionamento/histórico — campo direto na entidade é mais simples que uma tabela 1:1 extra sem ganho real nesta fase | Architect, task docs/tasks/039-sistema-planos-onboarding-completo.md |
| 2026-08-19 | Sprint 11 (040) — `OrganizationDto`/`GetOrganizationQuery`/`UpdateOrganizationCommand` moram em `Identity.Application.DTOs`/`Queries`/`Commands`, não em `Identity.Contracts` | `Identity.Contracts` é fronteira cross-module (consumida por OUTROS módulos, ex. `ICurrentUserAccessor`); DTOs devolvidos direto ao controller seguem o padrão já usado por `CreateOrganizationResultDto`/`MeResultDto`, dentro de `Identity.Application` mesmo — `Identity.Application` não referencia `Identity.Contracts` de volta | Dev Backend, task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (040) — `PUT /api/organizations/{id}` exige `id == organization_id` do token (mesma defesa IDOR do resto do controller) | Mesmo padrão já usado em `POST .../invites` e `GET .../invites` | Dev Backend, task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (040, reaberta) — `PUT /api/branches/{id}` fechou o gap de IDOR: `UpdateBranchCommand` ganhou `OrganizationId` (do token, nunca da rota); handler devolve `Branch.NaoEncontrada` (404) tanto pra branch inexistente quanto pra branch de outra organization — resposta idêntica nos dois casos, pra não confirmar existência do guid alheio | Achado na própria implementação da 040 (registrado como risco aceito), pedido do usuário pra fechar na sessão seguinte. `DeactivateBranch` tinha o MESMO gap, fechado numa 2ª reabertura logo em seguida (ver linha abaixo) | Dev Backend, task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (040, 2ª reabertura) — `DELETE /api/branches/{id}` (Deactivate) fechou o MESMO gap de IDOR do `PUT`: `DeactivateBranchCommand` ganhou `OrganizationId` (do token), handler confere `branch.OrganizationId == request.OrganizationId` antes de desativar, mesmo `Branch.NaoEncontrada` (404) nos dois casos | Pedido explícito do usuário logo após fechar o `PUT` — mesmo raciocínio, endpoint irmão no mesmo controller | Dev Backend, task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (040) — `PlanCards` extraído como componente compartilhado entre onboarding (`SelectPlanStep`) e configurações (`SettingsPage`), com `activeTier` opcional | Mesmos 3 planos, mesmo grid — só a tela de configurações precisa distinguir "plano atual" (desabilitado, badge) de "escolher"; duplicar o card pros dois lugares repetiria o mesmo risco de drift já registrado pro par `StatusBadge`/`FaturaStatusBadge` (sprint-8) | Dev Frontend, task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (040) — Paleta dark trocada de teal-ciano+roxo+azul (task 032) pra verde+preto puro; `--color-on-brand` continua escuro (`#06170F`) | Pedido explícito do usuário. Branco sobre o novo `--color-brand` (`#22C55E`) dá ~2.3:1 — falha AA, mesmo problema que já existia com o teal antigo; mantido texto escuro sobre o brand (~9:1). Light não mudou — já era verde+branco desde a 024 | Product Owner (decisão do usuário), task docs/tasks/040-tela-configuracoes-paleta-verde-preto.md |
| 2026-08-19 | Sprint 11 (041) — `PasswordResetToken` copia o desenho de `Invite`/`RefreshToken` ponta a ponta (hash-only, gerador dedicado, notifier NO-OP, erro genérico anti-enumeração) em vez de inventar um mecanismo novo | Mesma classe de problema (link de uso único enviado por fora) já resolvida 2x no projeto — reusar o desenho reduz risco e mantém os 3 fluxos consistentes pra quem lê o código depois | Architect, task docs/tasks/041-remediacao-auditoria-pre-venda.md |
| 2026-08-19 | Sprint 11 (041) — limite de usuário por plano checado em `AcceptInviteCommandHandler`, não em `CreateInviteCommandHandler` | A `OrganizationMembership` só é criada/reativada no ACCEPT — convite pendente não ocupa vaga. Checar na criação do convite permitiria N convites pendentes que todos estourariam o limite quando aceitos depois; checar no accept é o momento exato em que o recurso limitado (vaga de usuário) é de fato consumido | Architect, task docs/tasks/041-remediacao-auditoria-pre-venda.md |
| 2026-08-19 | Sprint 11 (041) — cobrança da plataforma continua manual/por fora nesta rodada (não integrou Stripe de verdade) | Decisão do usuário: sem chave de API do Stripe disponível na sessão, mesmo em modo teste. Optou por aviso explícito na UI em vez de bloquear o fluxo de escolha de plano — dívida nomeada, já estava esqueleto desde a task 039 | Product Owner (decisão do usuário), task docs/tasks/041-remediacao-auditoria-pre-venda.md |
| 2026-08-19 | Sprint 11 (041) — `Subscriptions.UnitTests` criado do zero (módulo não tinha NENHUM teste de handler desde a task 039) | `SelectPlanCommandHandler` ganhou lógica de negócio real (validação de downgrade) pela primeira vez — não dava pra adicionar comportamento sem cobertura nenhuma no módulo | QA (Jubileu), task docs/tasks/041-remediacao-auditoria-pre-venda.md |
| 2026-08-20 | Sprint 11 (042) — Orquestração de "criar Profissional + convidar Dentista" e "aceitar convite + vincular Profissional" mora nos CONTROLLERS (Bootstrap), não em um módulo chamando o `IMediator`/Application de outro | `Scheduling` e `Identity` não têm porta cross-module nova pra isso — os dois `Application` layers continuam sem se conhecer. O controller (composition root) sequencia 2 comandos independentes de módulos diferentes; isso é literalmente o papel do Bootstrap, diferente de um módulo invocar `IMediator.Send` de um Command de outro módulo por dentro (que vazaria a fronteira "só *.Contracts") | Architect, task docs/tasks/042-cadastro-dentista-com-convite.md |
| 2026-08-20 | Sprint 11 (042) — Convite disparado na criação do Profissional é BEST-EFFORT: se falhar (ex.: email já é membro de outra role), o Profissional continua criado, o aviso vai no corpo da resposta (`ConviteAviso`), não em 400 | Cadastrar o recurso (Profissional) e conceder acesso (convite) são preocupações independentes pro usuário — falhar o cadastro inteiro porque o convite não coube seria pior UX que avisar e deixar o Owner decidir o que fazer depois | Product Owner (decisão do usuário), task docs/tasks/042-cadastro-dentista-com-convite.md |
| 2026-08-20 | Sprint 11 (042) — `Profissional.VincularUsuario` é idempotente: não sobrescreve um `UserId` já setado | Um segundo convite pro mesmo email (ex.: reenvio, ou 2 profissionais cadastrados com o mesmo email por engano) não pode roubar o vínculo de quem já é o usuário real — primeiro aceite vence | Dev Backend, task docs/tasks/042-cadastro-dentista-com-convite.md |
| 2026-08-20 | Sprint 11 (043) — `Deactivate*Profissional`/`*Sala` criados no backend por simetria com `Update*`, mas SEM botão na UI ainda | Desativar um recurso sem nenhum jeito de reativar pela tela (endpoint `Ativar()` existe no domínio, mas nenhum controller chama) seria uma armadilha — melhor expor os dois juntos numa rodada futura do que só "desativar sem volta" agora | Product Owner, task docs/tasks/043-editar-profissional-sala.md |
