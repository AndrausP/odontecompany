namespace Scheduling.Domain.Enums;

/// <summary>Vínculo contratual do profissional com a clínica — informativo, não afeta cálculo de folha (fora de escopo).</summary>
public enum TipoContrato
{
    Clt = 1,
    Pj = 2,
    Autonomo = 3
}
